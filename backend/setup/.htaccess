# El .htaccess padre aplica CSP default-src 'none' (correcto para JSON);
# esta página de setup es HTML y necesita sus estilos inline.
<IfModule mod_headers.c>
    Header set Content-Security-Policy "default-src 'none'; style-src 'unsafe-inline'; form-action 'self'"
</IfModule>
